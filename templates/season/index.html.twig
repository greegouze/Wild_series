{% extends 'base.html.twig' %}

{% block title %}Season index{% endblock %}

{% block body %}
<main class="container">
    <h1>Season index</h1>

    <table class="table table-hover">
        <thead class="bg-primary">
            <tr>
                <th>Numero</th>
                <th>Année</th>
                <th>action</th>
            </tr>
        </thead>
        <tbody>
        {% for season in seasons %}
            <tr>
                <td>{{ season.number }}</td>
                <td>{{ season.year }}</td>
                <td>
                    <a class="btn btn-primary btn-sm" href="{{ path('app_season_edit', {'id': season.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="5">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a class="btn btn-primary" href="{{ path('app_season_new') }}">Create new</a>
    </main>
{% endblock %}
